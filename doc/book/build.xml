<?xml version='1.0'?>
<!DOCTYPE article PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN"
"http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd"
>
<chapter id="build">
	<title>The build phase</title>
	<section id="targets">
		<title>Declaring new targets</title>
		<para>
			Since the Waf commands build, clean, install and uninstall execute the same function <emphasis>build</emphasis>, it is somehow necessary to isolate the declaration of the targets from the actual code that will create them. For example:
				<itemizedlist>
					<listitem>Execution control: targets are only produced during the calls to the commands <emphasis>build</emphasis> and <emphasis>install</emphasis></listitem>
					<listitem>Parallel target processing to accelerate the builds: multi-core systems</listitem>
					<listitem>Filesystem abstraction: fetching data from the network</listitem>
					<listitem>Extensibility: adding support for new compilers and new languages from user scripts</listitem>
					<listitem>Flexibility: changing the semantics of the declarations depending on the platform (operating system, compiler, etc)</listitem>
				</itemizedlist>
				These main contraints lead to the creation of an abstraction layer between the actual code execution (<emphasis>task</emphasis>) and the declaration (<emphasis>task generators</emphasis>). Here are the definitions:
				<itemizedlist>
					<listitem>Task: Abstract unit of data transformation which may be delegated for later execution. Task instances often present sequential constraints and require a scheduler to optimize the overall execution time (execution in parallel whenever possible).</listitem>
					<listitem>Task generator: Object part of the user interface (Waf scripts) used to create lots of task instances. The task generators handle global constraints across the tasks: access to the configuration sets, data sharing, operating system abstraction, error checking, etc</listitem>
				</itemizedlist>
		</para>

		<para>
			Here is a sample project script to illustrate the concepts:
			<programlisting language="python">
srcdir = '.'
blddir = 'build'

def configure(ctx):
	pass

def build(ctx):
	obj = ctx(target='test.txt', rule='touch ${TGT}')
	print('  ' + obj.__class__)
			</programlisting>

			The call <emphasis>ctx(...)</emphasis> is just a shortcut on the build context to create new task generator instances easily. The output will be the following:

			<programlisting language="sh">
waf distclean configure build build clean
'distclean' finished successfully (0.002s)
'configure' finished successfully (0.001s)
Waf: Entering directory `/tmp/smallproject/build'
  class 'TaskGen.task_gen'
[1/1] test.txt:  -> build/default/test.txt
Waf: Leaving directory `/tmp/smallproject/build'
'build' finished successfully (0.011s)
Waf: Entering directory `/tmp/smallproject/build'
  class 'TaskGen.task_gen'
Waf: Leaving directory `/tmp/smallproject/build'
'build' finished successfully (0.004s)
  class 'TaskGen.task_gen'
'clean' finished successfully (0.002s)
			</programlisting>

			The line <emphasis>test.txt</emphasis> corresponds to the execution of the actual task. The task generator was created during the execution of both <emphasis>build</emphasis> and <emphasis>clean</emphasis> (print statement). The task was only executed during the first build, and not during the clean step. Also, the task was not executed when the target was considered up-to-date (to know more, consult the next chapters).
		</para>

	</section>

	<section id="glob">
		<title>Finding folders and files through node objects</title>
		<para>
			In general, file system access are slow operations (listing files and folders, reading file permissions, etc). For this reason, Waf maintains an internal representation of the files and folders already explored in the form of a tree of Node instances. Two nodes may be accessed fron the build context:
			<itemizedlist>
				<listitem>root: node representing the root of the file system, or the folder containing the drive letters on win32 systems</listitem>
				<listitem>path: node representing the path of the script being read (path to the wscript file)</listitem>
			</itemizedlist>
			Nodes are often used for low-level operations such as extending Waf, and direct node manipulation is hardly ever necessary from user scripts.
			<programlisting language="python">
srcdir = '.'
blddir = 'build'

def configure(ctx):
	pass

def build(bld):
	print('current path %r' % bld.path)
	print('absolute path of the root node: %r' % bld.root.abspath())
	print('absolute path of the current node: %r' % bld.path.abspath())

	etc = bld.root.find_dir('/etc')
	var = bld.root.find_dir('/var')
	print('the node representing /etc %r: ' % etc)
	print('path from /var to /etc %r' % etc.relpath_gen(var))

	fstab = bld.root.find_resource('/etc/fstab')
	print('path to /etc/fstab: %r' % fstab.abspath())
			</programlisting>
			The following results would be observed upon execution on a unix-like system:
			<programlisting language="sh">
$ waf distclean configure build
'distclean' finished successfully (0.002s)
'configure' finished successfully (0.001s)
Waf: Entering directory `/tmp/smallproject/build'
current path dir:///tmp/smallproject
absolute path of the root node: '/'
absolute path of the current node: '/tmp/smallproject'
the node representing /etc dir:///etc:
path from /var to /etc '../etc'
path to /etc/fstab: '/etc/fstab'
Waf: Leaving directory `/tmp/smallproject/build'
'build' finished successfully (0.003s)
			</programlisting>
		</para>

		<para>
			Listing files and folders automatically reduces the needs for updating the scripts (files may be added, removed or renamed). A Node method called <emphasis>ant_glob</emphasis> enables searching for folders and files on the file system. Here is an illustration:
			<programlisting language="python">
srcdir = '.'
blddir = 'build'

def configure(ctx):
	pass

def build(bld):
	print(bld.path.ant_glob('wsc*'))<co id="ant-co1" linkends="ant1"/>
	print(bld.path.ant_glob('w?cr?p?'))<co id="ant-co2" linkends="ant2"/>
	print(bld.root<co id="ant-co3" linkends="ant3"/>.ant_glob('etc/**/g*'<co id="ant-co4" linkends="ant4"/>, dir=True, src=False, bld=False)) <co id="ant-co5" linkends="ant5"/>
			</programlisting>

			<calloutlist>
				<callout arearefs="ant-co1" id="ant1"><para>The method ant_glob is called on a node object, and not on the build context</para></callout>
				<callout arearefs="ant-co2" id="ant2"><para>The patterns may contain wildcards such as * or ?, but they are <ulink url="http://ant.apache.org/manual/CoreTypes/patternset.html">Ant patterns</ulink>, not regular expressions</para></callout>
				<callout arearefs="ant-co3" id="ant3"><para>Calling ant_glob on the file system root may be slow, and may give different results depending on the operating system</para></callout>
				<callout arearefs="ant-co4" id="ant4"><para>The <emphasis>**</emphasis> indicates to consider folders recursively. Use with care.</para></callout>
				<callout arearefs="ant-co5" id="ant5"><para>By default, only node representing source files are returned. It is possible to obtain folder nodes and build file nodes by turning on the appropriate options.</para></callout>
			</calloutlist>

			The execution output will be similar to the following:
			<programlisting language="sh">
$ waf
Waf: Entering directory `/tmp/smallproject/build'
wscript
wscript
etc/ghostscript etc/gconf etc/gconf/gconf.xml.mandatory etc/gnome-vfs-2.0 etc/gpm etc/gnupg etc/gre.d etc/gdm
Waf: Leaving directory `/tmp/smallproject/build'
'build' finished successfully (12.873s)
			</programlisting>
		</para>
	</section>


	<section id="installing">
		<title>Installing files</title>
		<para>
			Three build context methods are provided for installing files:
			<itemizedlist>
				<listitem>install_files: install several files in a folder</listitem>
				<listitem>install_as: install a target with a different name</listitem>
				<listitem>symlink_as: create a symbolic link on the platforms that support it</listitem>
			</itemizedlist>

			<programlisting language="python">
def build(bld):
	bld.install_files('${PREFIX}/include', ['a1.h', 'a2.h']) <co id="inst-co1" linkends="inst1"/>
	bld.install_as('${PREFIX}/dir/bar.png', 'foo.png') <co id="inst-co2" linkends="inst2"/>
	bld.symlink_as('${PREFIX}/lib/libfoo.so.1', 'libfoo.so.1.2.3') <co id="inst-co3" linkends="inst3"/>

	env_foo = bld.env.copy()
	env_foo.PREFIX = '/opt'
	bld.install_as('${PREFIX}/dir/test.png', 'foo.png', env=env_foo) <co id="inst-co4" linkends="inst4"/>

	start_dir = bld.path.find_dir('src/bar')
	bld.install_files('${PREFIX}/share', ['foo/a1.h'], cwd=start_dir, relative_trick=True) <co id="inst-co5" linkends="inst5"/>

	bld.install_files('${PREFIX}/share', start_dir.ant_glob('**/*.png'), cwd=start_dir, relative_trick=True) <co id="inst-co6" linkends="inst6"/>
			</programlisting>

			<calloutlist>
				<callout arearefs="inst-co1" id="inst1"><para>Install various files in the target destination</para></callout>
				<callout arearefs="inst-co2" id="inst2"><para>Install one file, changing its name</para></callout>
				<callout arearefs="inst-co3" id="inst3"><para>Create a symbolic link</para></callout>
				<callout arearefs="inst-co4" id="inst4"><para>Overridding the configuration set (<emphasis>env</emphasis> is optional in the three methods install_files, install_as and symlink_as)</para></callout>
				<callout arearefs="inst-co5" id="inst5"><para>Install src/bar/foo/a1.h as seen from the current script into ${PREFIX}/share/foo/a1.h</para></callout>
				<callout arearefs="inst-co6" id="inst6"><para>Install the png files recursively, preserving the folder structure read from src/bar/</para></callout>
			</calloutlist>
		</para>
	</section>


	<section id="pre_post_build">
		<title>Executing specific routines before or after the build</title>
			<para>
			User functions may be bound to be executed at two key moments during the build command (callbacks):
			<itemizedlist>
				<listitem>immediately before the build starts (bld.add_pre_fun)</listitem>
				<listitem>immediately after the build is completed successfully (bld.add_post_fun)</listitem>
			</itemizedlist>
			Here is how to execute a test after the build is finished:
			<programlisting language="python">
srcdir = '.'
blddir = 'build'

def set_options(ctx):
	ctx.add_option('--exe', action='store_true', default=False,
		help='execute the program after it is built')

def configure(ctx):
	pass

def pre(ctx): <co id="prepost-co1" linkends="prepost1"/>
	print ("before the build is started")

def post(ctx):
	import Options, Utils
	if Options.options.exe: <co id="prepost-co2" linkends="prepost2"/>
		Utils.exec_command('/sbin/ldconfig') <co id="prepost-co3" linkends="prepost3"/>

def build(ctx):
	ctx.add_pre_fun(pre) <co id="prepost-co4" linkends="prepost4"/>
	ctx.add_post_fun(post)
			</programlisting>

			<calloutlist>
				<callout arearefs="prepost-co1" id="prepost1"><para>The callbacks take the build context as unique parameter <emphasis>ctx</emphasis></para></callout>
				<callout arearefs="prepost-co2" id="prepost2"><para>Access to the command-line options</para></callout>
				<callout arearefs="prepost-co3" id="prepost3"><para>A common scenario is to call ldconfig after the files are installed.</para></callout>
				<callout arearefs="prepost-co4" id="prepost4"><para>Scheduling the functions for later execution. Remember that in Python, functions are objects too.</para></callout>
			</calloutlist>

			Upon execution, the following output will be produced:

			<programlisting language="sh">
$ waf distclean configure build --exe
'distclean' finished successfully (0.002s)
'configure' finished successfully (0.001s)
Waf: Entering directory `/tmp/smallproject/build'
before the build is started
Waf: Leaving directory `/tmp/smallproject/build'
hello
'build' finished successfully (0.008s)
			</programlisting>

			</para>
	</section>

</chapter>

