<?xml version='1.0'?>
<!DOCTYPE article PUBLIC "-//OASIS//DTD DocBook XML V4.3//EN"
"http://www.oasis-open.org/docbook/xml/4.3/docbookx.dtd"
>
<chapter id="configuration">
	<title>The configuration system</title>

	<section id="config_data">
		<title>Using configuration data</title>
		<para>
			Configuration data is held by instances of the class <emphasis>Environment</emphasis>. The class acts as a wrapper around Python dicts to handle serialization (in human-editable files) and copy-on-write efficiently. Instances of that class are used extensively within waf, and the instances are usually named 'env'. Here is how configuration data is usually stored:

			<programlisting language="python">
srcdir = '.'
blddir = 'build'

def configure(conf):
    conf.env['CXXFLAGS'] = ['-O2'] # key-based access
    conf.env.store('test.txt')
    conf.env.TEST        = 'test'  # attribute-based access

    new = conf.env.__class__()
    new.load('test.txt')

    print new
    print ""
    print conf.env
			</programlisting>

			The execution will result in the following output:

			<programlisting language="sh">
$ waf configure
'CXXFLAGS' ['-O2']
'PREFIX' '/usr/local'

'CXXFLAGS' ['-O2']
'PREFIX' '/usr/local'
'TEST' 'test'

$ cat test.txt
CXXFLAGS = ['-O2']
PREFIX = '/usr/local'

$ cat build/c4che/default.cache.py
CXXFLAGS = ['-O2']
PREFIX = '/usr/local'
TEST = 'test'
			</programlisting>

			The default configuration is stored to the cache file named <emphasis>default.cache.py</emphasis>. The configuration data will always contain the default variable PREFIX, which is used to indicate the default directory in which to install something (in case there is something to install). The cache file is written in pseudo-python, and may be edited manually if necessary.
		</para>

		<para>
			Although the conf.env object is similar to a Python dict, a few routines are present to ease value access:
			<programlisting language="python">
srcdir = '.'
blddir = 'build'

def configure(conf):
	conf.env.append_value('CXXFLAGS', ['-O2', '-g'])
	conf.env.CCFLAGS = ['-g']
	conf.env.append_unique('CCFLAGS', ['-g', '-O2'])
	conf.env.prepend_value('CCFLAGS', ['-O3'])
	print(conf.env)

	import Utils
	s = Utils.subst_vars('${CCFLAGS}', conf.env)
	print(s)

			</programlisting>

			Upon execution, the output will be the following:

			<programlisting language="sh">
$ waf configure
'CCFLAGS' ['-O3', '-g', '-O2']
'CXXFLAGS' ['-O2', '-g']
'PREFIX' '/usr/local'
-O3 -g -O2
			</programlisting>
		</para>
	</section>


	<section id="config_data_sharing">
		<title>Consulting configuration data during the build</title>
		<para>
			By splitting the configuration from the build step, it is possible to stop and look at the configuration data (build/c4che/default.cache.py). The data used during the build should use the command-line options as little as possible. To achieve this, a pattern similar to the following is usually applied:
			<programlisting language="python">
srcdir = '.'
blddir = 'build'

def set_options(ctx):
	ctx.add_option('--foo', action='store', default=False, help='Silly test')

def configure(ctx):
	import Options
	ctx.env.FOO = Options.options.foo
	ctx.find_program('touch', var='TOUCH', mandatory=True) # a configuration helper

def build(ctx):
	print(ctx.env.TOUCH)
	print(ctx.env.FOO)
			</programlisting>

			The default configuration data is automatically loaded during the build. Upon execution, the output will be similar to the following:

			<programlisting language="sh">
$ waf distclean configure build --foo=abcd
'distclean' finished successfully (0.002s)
Checking for program touch               : ok /usr/bin/touch
'configure' finished successfully (0.001s)
Waf: Entering directory `/comp/waf/demos/simple_scenarios/folders/build'
/usr/bin/touch
abcd
Waf: Leaving directory `/comp/waf/demos/simple_scenarios/folders/build'
'build' finished successfully (0.003s)
			</programlisting>

		</para>
	</section>


	<section id="config_helper_execution">
		<title>Launching and catching configuration exceptions</title>
		<para>
			Configuration helpers are methods provided by the conf object to help finding parameters, for example the method <emphasis>conf.find_program</emphasis>
			<programlisting language="python">
def configure(conf):
	conf.find_program('test')
			</programlisting>
			When a test fails, the exception <emphasis>Configure.ConfigurationException</emphasis> is raised. For example, such exceptions are thrown when a program could not be found:
			<programlisting language="python">
def configure(conf):
	import Configure
	try:
		conf.find_program('missing_app', mandatory=True)
	except Configuration.ConfigurationError:
		pass
	conf.find_program(['gjc', 'cjg'], mandatory=True)
			</programlisting>
			The output will be:
			<programlisting language="sh">
$ waf
Checking for program missing_app         : not found
Checking for program gjc,cjg             : not found
 error: The program ['gjc', 'cjg'] could not be found
			</programlisting>
			It is sometimes useful to raise such an exception manually by using <emphasis>conf.fatal</emphasis>:
			<programlisting language="python">
def configure(conf):
	conf.fatal("I'm sorry Dave, I'm afraid I can't do that")
			</programlisting>
			Which will display the same kind of error
			<programlisting language="sh">
$ waf configure
 error: I'm sorry Dave, I'm afraid I can't do that
$ echo $?
1
			</programlisting>
		</para>
	</section>

	<section id="loading_config_tools">
		<title>Loading Waf tools</title>
		<para>
			For efficiency reasons, the Waf core libraries do not provide support for programming languages. The configuration helpers are located in separate files called <emphasis>Waf tools</emphasis> and located under the folder <filename>wafadmin/Tools</filename>. The contributed tools, or the tools in testing phase are located under the folder <filename>wafadmin/3rdparty</filename>.</para>
		<para>
			Tools usually provide additional routines, and may modify existing classes to extend their behaviour. We will now demonstrate a very simple Waf tool named <filename>dang.py</filename>:
			<programlisting language="python">
from Configure import conftest

@conftest <co id="cconf-co1" linkends="cconf1"/>
def detect(conf):
    print('-> detect fron dang!')

@conftest <co id="cconf-co2" linkends="cconf2"/>
def test_hello(conf):
    print('-> test from dang!')

def build_hello(self):
    print('-> build hello from dang!')

import Build
Build.BuildContext.build_hello = build_hello <co id="cconf-co3" linkends="cconf3"/>
			</programlisting>

			<calloutlist>
				<callout arearefs="cconf-co2" id="cconf2"><para>The default detection method to execute when the tool is loaded is called <emphasis>detect</emphasis></para></callout>
				<callout arearefs="cconf-co2" id="cconf2"><para>The function having <emphasis>@conftest</emphasis> are bound to the <emphasis>configuration context class</emphasis></para></callout>
				<callout arearefs="cconf-co3" id="cconf3"><para>New methods may be bound at runtime onto the build context class</para></callout>
			</calloutlist>

			For loading a tool, the method <emphasis>check_tool</emphasis> must be used during the configuration. The same tools are then loaded during the build.
			<programlisting language="python">
srcdir = '.'
blddir = 'build'

#import config_c <co id="conf-co1" linkends="conf1"/>

def configure(conf):

	#import config_c # <co id="conf-co2" linkends="conf2"/>

    def is_check_cc_present():
        try:
            print('# method conf.check_cc %r' % conf.check_cc) <co id="conf-co3" linkends="conf3"/>
        except:
            print('# there is no method conf.check_cc')

    is_check_cc_present()
    conf.check_tool('config_c') # <co id="conf-co4" linkends="conf4"/>
    is_check_cc_present()

    conf.check_tool('dang', tooldir='.') # <co id="conf-co5" linkends="conf5"/>
    conf.check_tool('dang', tooldir='.', funs='test_hello') <co id="conf-co6" linkends="conf6"/>
	conf.check_tool('dang', tooldir='.', funs=[])

def build(bld):
    bld.build_hello() <co id="conf-co7" linkends="conf7"/>
			</programlisting>

			<calloutlist>
				<callout arearefs="conf-co1" id="conf1"><para>By loading a Waf tool at the root of a script, the detection routines will not be executed (discouraged)</para></callout>
				<callout arearefs="conf-co2" id="conf2"><para>By loading a Waf tool using 'import', the tool will not be loaded at build time (discouraged)</para></callout>
				<callout arearefs="conf-co3" id="conf3"><para>Just an example to show if the method <emphasis>check_cc</emphasis> is present on the configuration context class</para></callout>
				<callout arearefs="conf-co4" id="conf4"><para>Load the tool providing configuration routines for c and c++ support (the method check_cc)</para></callout>
				<callout arearefs="conf-co5" id="conf5"><para>When tools provide a method named <emphasis>detect</emphasis>, it is executed during <emphasis>conf.check_tool</emphasis></para></callout>
				<callout arearefs="conf-co6" id="conf6"><para>Use specific functions instead of calling the default</para></callout>
				<callout arearefs="conf-co7" id="conf7"><para>Use the build context method defined in dang.py</para></callout>
			</calloutlist>

			Upon execution, the output will be the following:

			<programlisting language="sh">
$ waf distclean configure build
'distclean' finished successfully (0.002s)
# there is no method conf.check_cc
# method conf.check_cc (ConfigurationContext.check_cc)
-> detect fron dang!
-> test from dang!
'configure' finished successfully (0.003s)
Waf: Entering directory `/tmp/smallproject/build'
-> build hello from dang!
Waf: Leaving directory `/tmp/smallproject/build'
'build' finished successfully (0.003s)
			</programlisting>
			To ease the creation of projects split into modules, conf.check_tool will not load the tools twice for the same environment and the same parameters.
		</para>

		<para>
			An error handler attached to the conf object is used for catching the Configuration exceptions and processing the errors. Here is how to replace the default configuration error handler by a custom method which may modify the list of tests, stop the evaluation, or re-raise the exception:
			<programlisting language="python">
import Configure, Constants
@conf
def error_handler(fun, exc):
	print('exception %r' % exc)
	# other optionals return values: Constants.CONTINUE or anything else to re-raise the exception
	return Constants.BREAK
			</programlisting>
			The following diagram illustrates the test execution loop performed from conf.check_tool
			<graphic format="png" fileref="conftest.png" align="center"/>
		</para>
	</section>


</chapter>

