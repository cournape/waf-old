#! /usr/bin/env waf configure
# Thomas Nagy, 2006

# detect the caml variables and create an environment for it
add_option('--where-is-ocaml', type='string', help='ocaml directory', dest='ocamldir')

env = create_env()
env.detect('Ocaml')
env.store('ocaml.cache.py')

