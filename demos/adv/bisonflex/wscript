#!/usr/bin/env python
# encoding: utf-8

def build(bld):
	# to compile in c or cpp mode, uncomment the appropriate line below:
	mode = 'cpp'
	#mode = 'cc'

	obj = bld.createObj(mode, 'program')
	obj.source='''
	main.c
	calc.l
	calc.y
	'''
	obj.includes='.'
	obj.target='calc'
	obj.uselib="CALC"
