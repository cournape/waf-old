#! /usr/bin/env python
# encoding: utf-8
# Thomas Nagy, 2006 (ita)

def build(bld):

	# simple program

	obj = bld.new_task_gen(
		features = 'ocaml',
		kind = 'all',
		includes = '.',
		target = 'camlprog')

	obj.find_sources_in_dirs('.')

